{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window/event access for the shell window.",
  "windows": ["*"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}
