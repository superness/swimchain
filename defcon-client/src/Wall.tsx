/**
 * Stub — the wall lands in the next task (Task 7: post list, composer,
 * replies, all against `VITE_DEFCON_SPACE`). This placeholder exists so
 * BrowserJoin has somewhere to hand off to on a successful join.
 */
export function Wall() {
  return (
    <section className="wall">
      <p className="eyebrow">@defcon34</p>
      <h2>You're in.</h2>
      <p className="lede">The wall lands in the next task.</p>
    </section>
  );
}
