node_modules
dist
*.tsbuildinfo

# Regtest/dev config — real values live only in .env.production, filled in at
# go-live per the ops runbook. Never commit a developer's local node config.
.env.development
.env.local
.env.*.local

# tsconfig.node.json emit byproducts (should be prevented by noEmit, kept as a
# safety net in case that setting ever regresses).
/vite.config.js
/vite.config.js.map
/vite.config.d.ts
/vite.config.d.ts.map
