# BVT run — 2026-07-16 (~19:45Z)

Post-consensus-fix round: fork-race fixes (#80), reply-index + client hardening
(#81, #82 pending merge), retunable reefs, provisional tides. Focus: **multi-client
cross-communication with visual review**.

## Tier 1 (`bvt.sh --e2e`) — 9 pass / 1 stale-fail

| ✓ | Note |
|---|---|
| A1 | converged @939 |
| A2 | 723 block-activity lines / 2h |
| A3 | **FAIL (stale)** — guard-hits=137/24h counts yesterday's chaos; **0 hits in the last 3h** (verified directly) |
| A4 | store sanity ok |
| A5 | **tie-reorgs=1/6h** (was 16 before the eligibility-window fix — consensus fix confirmed in production) |
| B1-B4 | website, gateway RPC, faucet slots (9), search |
| B5 | e2e sponsorship: SPONSORED in 31s via gateway |

## Cross-communication matrix (all verified by screenshot)

One thread, three clients, two nodes, three identities:

1. **forum @ qa2** (SquallSeeker) creates "BVT cross-comm 1916" in Latency Lab 2 — byline name+address+copy, 100% health.
2. **qa3 node RPC** (independent node, fresh identity 31f80e6c…) lists the post with full body via `list_space_posts` seconds later.
3. **feed @ qa3** — fresh profile shows honest empty feed → Discover finds the space by name → Follow → post appears → **reply submitted** (PoW in browser).
4. **forum @ qa2** shows the qa3 reply, attributed, thread stats updated.
5. **chat @ qa2** renders the same thread as `#bvt-cross-comm-1916` with all messages; a **chat message** posted into it completes the tri-client conversation.
6. **search @ qa2** finds the fresh thread with highlights (self-authored index).
7. qa3's fresh identity was already sponsored by the fresh-node flow; sponsor-status RPC confirmed.

## UI sweep

- U1 feed ✅ · U2 Discover ✅ (names resolve; private "Morning Study Vault" decrypts) · U3 post detail ✅ (space chip named)
- U4 forum ✅ · U5 chat ✅ (no literal `**`; thread→channel explosion = known-red, unchanged)
- U6 search ✅ · U7 wiki ✅ (Synced, Minecraft ns, no follows-copy)
- U9 live reef / U10 live chess landings ✅ (fresh-profile identity funnel)
- U12 phone ✅ — new APK, playing the NEW reef ("The Open Reef", epochMoves=17), **Season 3 champion banner correct**, settling-cell glow, current HUD copy
- U8 hide-space: skipped (no changes in that path this round)
- U11 launcher: skipped (sidecar binary updated today; full launcher pass deferred)

## New findings (minor)

1. **Phone: raw "Failed to fetch" banner lingers** over a working board — transient
   poll error sticks in the error slot until the next failure clears it; should
   auto-clear on the next successful poll + get friendlier copy.
2. **Wiki Recent Changes says "just now" for 2-day-old edits** — timestamp source
   suspect (action-time vs authoring-time class, again).
3. `scripts/deploy-web-clients.sh` post-deploy live check flakes (~50%) despite
   retries; manual curl always passes seconds later — rework check to verify via
   ssh on both hosts instead of racing nginx over HTTPS.

## Known-red (unchanged, do not re-file)

M1 raw identifiers on unresolved surfaces (feed cards, search cards, wiki rows) ·
chat thread→channel explosion · search indexes only self-authored content ·
client-fix distribution gap (hosted installers/APK release publication awaits
operator authorization).
