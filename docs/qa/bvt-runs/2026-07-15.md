# BVT run — 2026-07-15 ~04:18Z (post launch-blocker deploy)

Context: fleet deployed with the four launch-blocker fixes
(`e333e4c9` formation gate, `b94516eb` real chain weight, `9cb2dda8`
auth lockout, `516a9eba` unnamed-space filter). Rolled client2 → bot →
seed; every node's formation gate opened on peer-tip parity at restart
(no solo-formation window). Guarded release APK signed and installed on
the test phone (fresh wipe + resync).

## Tier 1 (`bash scripts/bvt.sh --e2e --failover`)

```
A1    PASS    converged at height 78
A4    PASS    root_blocks >= height, RPC responsive
A2    PASS    block activity in last 2h (656 log lines)
A3    FAIL    cascading=10 guard-hits=1 (24h, seed)
B1    PASS    /=200 /reef/=200 /chess/=200 /example/=200 /download=200
B2    PASS    4 named spaces via gateway
B3    PASS    faucet auto-approve slots: 5
B4    PASS    space search hits: 1
B5    PASS    SPONSORED a12ab38d24e3 in 71s
B6    PASS    failover answered from node 11d8015e
=== 9 pass, 1 fail ===
```

**A3 note:** the 10 cascading-rollback lines are residue of the
2026-07-14 evening incident (pre-guard) still inside the 24 h window;
zero new rollbacks since the deploy (verified against the deploy
timestamp 04:08Z). Expect A3 green once the window rolls past the
incident (~2026-07-15 evening). B5 passing proves block formation works
end-to-end with the gate live fleet-wide.

## Tier 2 — full sweep (~12:30–13:15Z, qa2 + live sites; fixes `fa9d9aa3`)

| Item | Result | Note |
|---|---|---|
| U1 feed home | PASS | renders; empty feed CORRECT (verified `list_followed_spaces` = [] for qa2's identity) |
| U2 feed Discover | PASS | private "Morning Study Vault" decrypts; only NAMED spaces listed (new filter); "Newbies" resolved live via the new resolve-nudge and appeared |
| U3 feed post detail | PASS | space chip shows name; byline shows raw address — CORRECT, the author (faucet identity) has no display name set (cosmetic: give the faucet a name) |
| U4 forum thread | PASS after fix | was UNRUNNABLE: baked-in VITE_RPC_PORT (forum/.env.local) outranked the shell's injected config → dialed 19736 with no auth, hung on "Loading identity…". Fixed priority (`fa9d9aa3`). Post-fix: Synced/3 peers, byline+copy, 100% health bar, dark inputs. Nit: breadcrumb shows raw space id even when the name is resolved |
| U5 chat | PASS | named servers only; private name decrypts; DM list populates; no literal `**`; thread→channel explosion unchanged (known) |
| U6 search | PASS w/ known-red | "commons" no local hit (search indexes only self-authored content — pre-existing confirmed bug — and qa2 lacks seed-only names); positive control: "latency" suggests "Latency Lab 2" instantly; network-facing search covered by B4 via gateway |
| U7 wiki | PASS after fix | loads, Synced; Recent Changes empty-state mentioned follows → copy fixed (`fa9d9aa3`) |
| U8 hide-space | PASS | context menu renders; hide removes from Discover; Settings→Hidden Spaces lists it BY NAME; Unhide clears node-side pref (verified via list_spaces) — UI shows it after the ≤5 min client cache rolls (nit) |
| U9 live reef | PASS | landing renders (funnel proven by B5) |
| U10 live chess | PASS | landing renders |
| U11 launcher | **PASS** (completed ~13:52Z with operator password) | unlock renders; post-unlock: Connected · TESTNET · 3 peers, identity chip, 5 registry tiles (chat/feed/forum/search/wiki); Feed tile spawned `feed-app.exe`. L1 refined: `sw.exe` exits 3 for BOTH wrong-password and corruption ("Decryption failed - wrong password?" on stderr), so the shell can't distinguish — fix is a distinct exit code / stderr marker mapped to a "wrong password" message. Also note `sw identity show` does NOT verify the password (prints plaintext public fields) — don't use it as a decryption check |
| U12 phone parity | **PASS** (last night) — status bar `Running · 3 peers · height 78 · 100%` on the guarded release APK (fresh identity post-wipe, unsponsored banner expected) |

## Distribution (~13:30Z, operator-authorized)

The client-fix distribution gap is CLOSED for this round:
- **Desktop installer rebuilt** (guarded `sw.exe` sidecar + all fixed
  client bundles, verify-clients OK) and uploaded to seed + gateway
  `/var/www/swimchain/` — the previous hosted installer was from Jul 4,
  PRE-incident. Verified live: `https://swimchain.io/download/windows`
  serves the new 27,800,954-byte exe (byte-exact).
- **Android v0.1.1-alpha** released on GitHub (same signing key,
  in-place update; `.so` verified to embed the post-fix feed bundle) and
  `/download/android` updated on both hosts. Asset verified live
  (18,298,460 bytes). Phone updated in place: `Running · 3 peers ·
  height 92 · 100%`, data preserved.

## Stress test — multi-client action storm (~14:27–14:40Z)

Coordinated storm from 5 authoring identities / 7 concurrent workers, each
submitting REAL client actions (same PoW + canonical signatures) via
`tools/swim-bot/storm.mjs`: react/reply/post/create-space/profile/reef-game-move.
Sources: **local PC** (qa2, cookie, 2w), **phone** (signature auth via the
node's own sign_message, 1w), **client2 droplet** (cookie, 2w), **bot droplet**
(faucet identity, cookie, 2w). Seed is unsponsored → relay only (expected).

**Throughput:** 262 actions accepted, 3 failed (all RPC client-side timeouts
under load, ~1.1%; no consensus/auth failures).

| Source | OK | Fail | Auth |
|---|---|---|---|
| qa2 (local PC) | 104 | 1 | cookie |
| phone (mobile) | 75 | 0 | signature |
| client2 (droplet) | 49 | 1 | cookie |
| bot/faucet (droplet) | 31 | 1 | cookie |

All six action types exercised by every source (e.g. qa2 mix:
react 40 / reply 26 / game 14 / post 10 / profile 9 / space 5).

**Chain under load (12 min):** height 100 → 119 (+19 blocks; ~1.6/min vs the
quiet ~0.1/min), content_blocks 137 → 285, registered_spaces → 17. All four
nodes tracked together; one transient 1-block lag (seed 114 vs 115) self-healed
within the tick (seed 114→116 in seconds, same chain).

**Consensus behavior (seed journal, storm window) — the payoff:**
- **16 tip reorgs** via `heavier than tip (real chain weight)` — NORMAL,
  healthy fork resolution: multiple producers race blocks at the same height,
  each resolved on real walk-parents weight and bounded to the contested tip
  height (`Fork point at height N — unmarking canonical heights N+1..=N`).
- **0 cascading rollbacks** — the 2026-07-14 incident class, absent under the
  heaviest load we've run.
- **0 deep-fork guard hits, 0 formation-gate defers** (nodes stayed synced, so
  the gate correctly PERMITTED formation), **0 auth lockouts (-32017)** despite
  262 authed submissions across cookie + signature paths.

Verdict: the four launch-blocker fixes (formation gate, real chain weight +
bounded unmark, auth lockout) hold under concurrent multi-client load. Tip-reorg
churn is expected and self-resolving; the pathological cascading rollback did
not occur. Driver committed at `tools/swim-bot/storm.mjs`.

## UX fixes + full redistribution (~15:00–16:00Z)

Two UX bugs fixed, then a FULL rebuild+redistribute (the launcher's
standalone app exes were from Jul-13 and predated EVERY client fix this
session — the plain `tauri:build` would have shipped stale forum/feed):

- **L1 launcher "corrupted identity" scare** (`6e23515d`): the CLI
  returned exit code 3 for wrong-password AND corruption; now wrong
  password is its own code 5, launcher maps it to "Incorrect password.
  Please try again." Verified on the rebuilt launcher (screenshot).
- **Forum thread breadcrumb** showed the raw space id; now resolves the
  name ("Spaces / Latency Lab 2 / <title>"). Verified via swim-auto.

Full build via `build.ps1` (not `tauri:build`) rebuilt all 5 launcher
app exes (app-shell + fresh client each) so forum-app.exe/feed-app.exe/
etc. carry this session's fixes. Verified forum-app.exe embeds the
breadcrumb bundle (`index-BDGaPjrD`) and the rebuilt launcher unlocks →
5 registry tiles → Forum tile spawns forum-app.exe.

Distributed + verified live:
- **Desktop installer** rebuilt (25,515,052 B) → seed + gateway →
  `https://swimchain.io/download/windows` serves it byte-exact.
- **Mobile v0.1.2-alpha**: .so rebuilt with current feed (resolve-nudge
  + config-priority), signed (SHA-256 1dc5478e…), GitHub release cut,
  download-android.html updated + deployed. Phone installed in place →
  **height 120, 2 peers, 100%**, rendering storm content with resolved
  profile display names. APK asset live (18,298,460 B).

**Sanity findings (non-blocking):**
- The 24-post space (`sp1qqqsqqpm8shn3`) has NO name anywhere on the
  network (even the seed) — with the new unnamed-space filter it is now
  hidden in all clients, by operator request. Name it on-chain if it
  should be browsable.
- Reef/Chess/Daily-Drift names remain seed-config-only (documented
  07-14): GET_SPACE_META has nothing to serve, so other nodes hide those
  spaces from lists. Same cleanup path as above if that's not desired.
- Faucet identity has no display name → raw-address bylines on all bot
  posts. One `set_identity_name` on the bot droplet fixes it.

## Live formation-gate evidence

- Isolated node (dead-proxy `--proxy-only`): `Deferring block formation:
  no synced peer yet … grace expires in 87s` → `Formation gate OPEN:
  grace window (90s) expired` — 0 blocks formed while closed.
- Fleet-connected node: deferred through sync → `Formation gate OPEN:
  synced with peer tip (our height 76 >= best peer height 76)`.
- All three droplets + phone logged parity-open on restart.
