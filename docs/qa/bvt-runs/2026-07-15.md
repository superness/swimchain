# BVT run — 2026-07-15 ~04:18Z (post launch-blocker deploy)

Context: fleet deployed with the four launch-blocker fixes
(`e333e4c9` formation gate, `b94516eb` real chain weight, `9cb2dda8`
auth lockout, `516a9eba` unnamed-space filter). Rolled client2 → bot →
seed; every node's formation gate opened on peer-tip parity at restart
(no solo-formation window). Guarded release APK signed and installed on
the test phone (fresh wipe + resync).

## Tier 1 (`bash scripts/bvt.sh --e2e --failover`)

```
A1    PASS    converged at height 78
A4    PASS    root_blocks >= height, RPC responsive
A2    PASS    block activity in last 2h (656 log lines)
A3    FAIL    cascading=10 guard-hits=1 (24h, seed)
B1    PASS    /=200 /reef/=200 /chess/=200 /example/=200 /download=200
B2    PASS    4 named spaces via gateway
B3    PASS    faucet auto-approve slots: 5
B4    PASS    space search hits: 1
B5    PASS    SPONSORED a12ab38d24e3 in 71s
B6    PASS    failover answered from node 11d8015e
=== 9 pass, 1 fail ===
```

**A3 note:** the 10 cascading-rollback lines are residue of the
2026-07-14 evening incident (pre-guard) still inside the 24 h window;
zero new rollbacks since the deploy (verified against the deploy
timestamp 04:08Z). Expect A3 green once the window rolls past the
incident (~2026-07-15 evening). B5 passing proves block formation works
end-to-end with the gate live fleet-wide.

## Tier 2 — full sweep (~12:30–13:15Z, qa2 + live sites; fixes `fa9d9aa3`)

| Item | Result | Note |
|---|---|---|
| U1 feed home | PASS | renders; empty feed CORRECT (verified `list_followed_spaces` = [] for qa2's identity) |
| U2 feed Discover | PASS | private "Morning Study Vault" decrypts; only NAMED spaces listed (new filter); "Newbies" resolved live via the new resolve-nudge and appeared |
| U3 feed post detail | PASS | space chip shows name; byline shows raw address — CORRECT, the author (faucet identity) has no display name set (cosmetic: give the faucet a name) |
| U4 forum thread | PASS after fix | was UNRUNNABLE: baked-in VITE_RPC_PORT (forum/.env.local) outranked the shell's injected config → dialed 19736 with no auth, hung on "Loading identity…". Fixed priority (`fa9d9aa3`). Post-fix: Synced/3 peers, byline+copy, 100% health bar, dark inputs. Nit: breadcrumb shows raw space id even when the name is resolved |
| U5 chat | PASS | named servers only; private name decrypts; DM list populates; no literal `**`; thread→channel explosion unchanged (known) |
| U6 search | PASS w/ known-red | "commons" no local hit (search indexes only self-authored content — pre-existing confirmed bug — and qa2 lacks seed-only names); positive control: "latency" suggests "Latency Lab 2" instantly; network-facing search covered by B4 via gateway |
| U7 wiki | PASS after fix | loads, Synced; Recent Changes empty-state mentioned follows → copy fixed (`fa9d9aa3`) |
| U8 hide-space | PASS | context menu renders; hide removes from Discover; Settings→Hidden Spaces lists it BY NAME; Unhide clears node-side pref (verified via list_spaces) — UI shows it after the ≤5 min client cache rolls (nit) |
| U9 live reef | PASS | landing renders (funnel proven by B5) |
| U10 live chess | PASS | landing renders |
| U11 launcher | PARTIAL | unlock screen renders (address, network select, password); unlock attempt with wrong password reproduces known-red L1 ("Node failed to start (exit code: 3)… corrupted identity" for a bad password); registry tiles NOT verified — needs the operator's launcher password |
| U12 phone parity | **PASS** (last night) — status bar `Running · 3 peers · height 78 · 100%` on the guarded release APK (fresh identity post-wipe, unsponsored banner expected) |

**Sanity findings (non-blocking):**
- The 24-post space (`sp1qqqsqqpm8shn3`) has NO name anywhere on the
  network (even the seed) — with the new unnamed-space filter it is now
  hidden in all clients, by operator request. Name it on-chain if it
  should be browsable.
- Reef/Chess/Daily-Drift names remain seed-config-only (documented
  07-14): GET_SPACE_META has nothing to serve, so other nodes hide those
  spaces from lists. Same cleanup path as above if that's not desired.
- Faucet identity has no display name → raw-address bylines on all bot
  posts. One `set_identity_name` on the bot droplet fixes it.

## Live formation-gate evidence

- Isolated node (dead-proxy `--proxy-only`): `Deferring block formation:
  no synced peer yet … grace expires in 87s` → `Formation gate OPEN:
  grace window (90s) expired` — 0 blocks formed while closed.
- Fleet-connected node: deferred through sync → `Formation gate OPEN:
  synced with peer tip (our height 76 >= best peer height 76)`.
- All three droplets + phone logged parity-open on restart.
