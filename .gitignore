# Generated by Cargo
/target/

# Cargo.lock for library crates (keep for binary crates)
# Uncomment if this becomes library-only:
# Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.rs.bk

# Benchmark data (regenerated)
/target/criterion/

# Local configuration
.env
.env.local
config.local.toml

# Chat history (local only)
.chat-history*.md

# Claude Code local data
.claude/

# Node modules (if any)
node_modules/
package-lock.json

# Temporary files from pipelines
finaloutputtehsis.txt

# Screenshot/design files (keep in separate design repo or remove)
*.png

# Draft whitepapers (keep only final version)
SWIMCHAIN_ACADEMIC_WHITEPAPER.md
SWIMCHAIN_ACADEMIC_WHITEPAPER_v2.md
SWIMCHAIN_PROTOCOL_WHITEPAPER.md
SWIMCHAIN_WHITEPAPER.md
SWIMCHAIN_WHITEPAPER_ACADEMIC.md
WHITEPAPER.md
ACADEMIC_WHITEPAPER.md

# Keep only the consolidated whitepaper in docs/
desktop-app/public/clients/
tools/dns-seeder/swimchain-dns-seeder
*.tsbuildinfo

docs/qa/node_modules/
docs/qa/package*.json

# Local node data dirs (identity keys, chain state)
/genesis/
/genesis-testnet/
/node-genesis.log
/.daemon-pids/swim-auto.log
/target-test/
swimchain-wasm/pkg/

# Nested build outputs (per-app Cargo/Tauri target dirs)
**/target/
**/target-test/

# Daemon runtime files (pids + per-run logs)
/.daemon-pids/*.log
/.daemon-pids/*.pid

# Build / run logs
apk-build.log
desktop-build.log
/node-genesis.err.log
/node-genesis.out.log

# Desktop app build output & bundled binaries (regenerated)
desktop-app/dist/
desktop-app/src-tauri/apps/
desktop-app/src-tauri/binaries/

# Local test/QA node data dirs (identity keys, chain state)
/qa*-testnet/
/swim-clean-testnet/
/valreg-regtest/

# Generated wiki-import output
tools/wiki-import/out-mc-wikipedia/
